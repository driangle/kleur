## 2026-04-05T13:15:50Z

Started task 01knew747.

**Approach:** Inventory all throw sites and existing custom errors first, then replace any generic errors with package-declared error classes, update exports/docs, and add tests for affected public paths.

**Next:** Audit source files for thrown errors and current error class coverage.

## 2026-04-05T13:18:10Z

Completed task 01knew747.

**Completed:**

- [x] Added a dedicated `ts/src/errors.ts` module with package-owned error subclasses for parse and distance validation failures
- [x] Replaced library `throw new Error(...)` sites in `parse.ts` and `distance.ts` with exported custom error classes
- [x] Exported the new error classes from the package root and documented the rule in `AGENTS.md` and `CLAUDE.md`
- [x] Updated parse and distance tests to assert specific error types, and added coverage for previously untested distance validation branches
- [x] Ran `make check` successfully

**Notes:** Remaining `throw new Error(...)` usage is limited to a test-only fallback in `ts/tests/vectors.test.ts`, not library runtime code.
